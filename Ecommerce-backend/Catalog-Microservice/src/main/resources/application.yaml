server:
  port: 8082

spring:
  application:
    name: Catalog-Microservice

  datasource:
    url: jdbc:h2:mem:testdb

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
