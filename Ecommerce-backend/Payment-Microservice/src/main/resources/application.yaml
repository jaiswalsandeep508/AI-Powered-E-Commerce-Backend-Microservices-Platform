server:
  port: 8086

spring:
  application:
    name: Payment-Microservice
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update