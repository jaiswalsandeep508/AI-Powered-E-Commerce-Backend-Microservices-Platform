server:
  port: 8087

spring:
  application:
    name: Notification-Microservice
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update