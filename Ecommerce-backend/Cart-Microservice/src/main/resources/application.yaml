server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: Cart-Microservice

  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update